import { MatmenuforDirective } from './matmenufor.directive';

describe('MatmenuforDirective', () => {
  it('should create an instance', () => {
    const directive = new MatmenuforDirective();
    expect(directive).toBeTruthy();
  });
});
